# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/*
wheels/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ===== Local scratch / extracted reference docs =====
.tmp/

# 虚拟环境
.venv/
.venv-*/
env/
venv/
app/.venv/

# ===== 前端 / Node =====
node_modules/
web/dist/
web/dist-ssr/
*.local
.eslintcache
*.tsbuildinfo

# ===== Native / JUCE Host 本地构建产物 =====
native/juce-vst3-host/build/
native/juce-vst3-host/build-*/
native/juce-vst3-host/out/
native/juce-vst3-host/.vs/
native/juce-vst3-host/CMakeUserPresets.json

# ===== 模型 / 音频 等大文件 =====
*.pth
*.onnx
*.pt
*.ckpt
*.safetensors
*.mp3
*.wav
*.flac
*.m4a
*.ogg
*.aac

# ===== 日志 =====
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ===== 编辑器 / IDE =====
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ===== 操作系统 =====
.DS_Store
Thumbs.db
desktop.ini

# ===== 安装器生成的引擎与模型（开箱即用，由 install.ps1 重建）=====
engines/
models/

# ===== 本地用户数据（默认在 ~/.xb-svcb，仅在被移入仓库时生效）=====
.xb-svcb/
.venv-svc/

.vscode

# ===== 例外：已有自带模型继续纳入版本库 =====
# 保留安装器要附带的 SVC / RVC 底模与 UVR 模型；大文件通过 Git LFS 管理。
# 这些例外要放在最后，覆盖前面的 dist/*、*.pt、*.pth 等忽略规则。
#
# 注意：单文件体积很大（安装器约 2GB、ContentVec 1.27GB），GitHub 单文件上限 100MB，
#       直接 push 会被拒绝。若要托管到 GitHub，请用 Git LFS 跟踪这些大文件：
#         git lfs install
#         git lfs track "assets/models/**" "dist/XB-SVCB-Setup.exe"
#       （或仅本地/内网仓库保留，发布走 GitHub Releases 等外部分发）

# 自带模型（安装器附带资源，走 Git LFS）
!assets/models/
!assets/models/**

# SeedVC 离线权重仅保留在本机构建环境，并通过 GitHub Release 安装分卷发布。
# JSON 配置仍纳入版本库，避免源码缺少模型结构与预处理配置。
assets/models/seedvc/**/*.bin
assets/models/seedvc/**/*.pt
assets/models/seedvc/**/*.safetensors

# 安装器本体不入库：超过 GitHub LFS / Release 之外的 2GiB 单文件上限处理，
# 改为构建后上传到 GitHub Releases 分发（见 README）。dist/* 已忽略，无需额外规则。
