## v0.0.26 · 参考人声动态对齐与自然美声升级

> 本次更新把这一轮 AI 修音、参考人声动态对齐、角色音色、美声母带、界面/API 参数、文件清理和安装器适配统一归入 **v0.0.26**。重点是在保留目标角色音色的同时，减少机械感、电音感、细微杂音和持续元音的周期性调制，让 AI 翻唱更接近自然演唱。

> [!IMPORTANT]
> v0.0.26 继续使用分卷安装包。请同时下载 `XB-SVCB-Setup.exe` 和同版本的全部 `XB-SVCB-Setup-*.bin`，放在同一目录后再运行安装程序；缺少任意分卷都无法完整释放内置资源。

### 🎙️ 多推理引擎自然度优化

- So-VITS-SVC、RVC、SeedVC 与 DDSP-SVC 的原始推理结果统一接入源人声自然度保护。源人声只提供响度包络和静音时间，不混入任何原唱波形或频谱内容，目标角色音色仍完全来自当前 AI 模型。
- 句内空隙会在 380 ms（0.38 秒）以内自然桥接，并为起音增加约 70 ms 前视、为尾音保留约 240 ms 延续。较长无声区按引擎动态衰减 12–16 dB，超过 0.5 秒的数字静音恢复为精确零，减少声码器底噪和明显的 AI 式停顿。
- 原干声与模型输出以 10 ms 窗口比较局部包络，仅通过最大约 -3/+2 dB 的平滑增益恢复微动态，不执行音色混合、时间拉伸或额外 F0 量化。
- 默认推理会按输出采样率精确补零或裁尾，使总时长与源干声一致，避免多模型拼接积累毫秒级尾部错位。
- So-VITS-SVC 的随机声码器噪声由 0.4 降至 0.25，切片上下文由 0.5 秒增加到 0.75 秒；30 秒显存切片新增 120 ms 重叠淡化，并让模型输出保留 35% 的源人声响度包络，减少接缝、忽大忽小和电子毛刺。
- SeedVC 的可调质量范围由 10–50 步调整为 20–50 步，默认由 30 步提高到 35 步；CFG 从 0.7 降至 0.55，减轻过强音色条件导致的金属感和过度平滑。
- RVC 的索引率、清辅音保护及 DDSP-SVC 的共振峰参数继续尊重用户设置。

### 🎼 参考人声动态对齐与自然修音

- 新增“参考人声动态对齐”：先根据停顿切分并单调匹配句段，再以说话人归一化的倒谱与动态声学特征执行受限音素 DTW，最后将匹配结果用于局部节奏判断和参考 F0 对应。
- 密集音素锚点约每 300 ms 采样一次，继续为自然修音提供字音级参考关系，但不再直接驱动持续变化的波形时间拉伸。
- 只有原始局部节奏偏差超过 25 ms、按用户强度应用后仍达到 8 ms，且附近至少三个锚点方向一致时，才会创建 Praat `DurationTier`。小幅或孤立偏差保留原时序，避免元音出现类似对着电风扇唱歌的颤动。
- 所有时间校正均受用户强度和最大伸缩比例限制，起点、终点与整轨总时长保持不变；时间映射强制单调，避免结尾重复时间点或局部倒序。
- 对齐后的参考 F0 通过 Praat `PitchTier` 执行自然修音：过滤八度误检、弱化硬性半音吸附、限制最大音高偏移，并在乐句边缘渐入渐出，保留颤音、滑音、清辅音和自然收尾。
- Praat PSOLA 只在连续人声区域参与混合。0.45 秒以内的句内空隙保持连续，长数字静音恢复原始样本，避免重合成在空白处留下低频或 DC 底噪。
- AI 对齐与自然修音分别可调，默认均为 45%；参考 F0 的局部时间映射同样服从 AI 对齐强度，不会绕过滑块全量应用。
- 当参考人声相关性不足、Praat 环境不可用或自然修音失败时，系统会保留原 AI 人声继续后续增强，不阻塞整项任务。

### 🎚️ AI 美声与角色音色

- 参考人声用于句段/音素对应、局部节奏与 F0 引导，并在高级层提供受限的宽带平衡和真实辅音/呼吸细节；不会把原唱的中频身份直接复制给 AI 角色。
- 新增 **AI 角色共振峰**（默认 60%）：只分析转换后目标声音自身 250 Hz–4.5 kHz 的稳定宽带峰，强化当前模型角色的音色辨识度；实际增益随局部人声置信度平滑收放，不强化静音和弱呼吸。
- 新增 **AI EQ**（默认 55%）：按整轨有效人声的 body、mud、presence 和 air 分布推导小幅校正，再按 20 ms 局部人声窗口动态混合，不对整轨施加恒定处理量。
- 新增 **AI Compressor**（默认 45%）：从当前素材的有效动态范围推导阈值、压缩比、attack 和 release，并按局部超阈值幅度动态混合，最后补偿有效人声响度。
- 新增 **AI Exciter**（默认 25%）：只添加少量高频谐波残差；局部高频或齿音占比升高时即时降低处理量，避免放大 AI 高频纹理。
- 新增 **Stereo**（默认 30%）：使用保持单声道中置信号的中侧扩展，仅对高频去相关；宽度随局部人声活动变化，并在高频占比突增时自动收窄。
- 新增 **AI 响度包络**（默认 58%）：从增强前的 AI 人声提取 20 ms 局部响度，以约 70 ms 平滑轮廓修正完整效果链造成的动态偏差；校正上限受限在 ±3 dB 并继续服从滑块强度，停顿、弱呼吸和残留底噪不会被抬高。
- 美声滑块现在代表“处理上限”，不再对应固定效果量。引擎会为每首素材动态推导高通截止、body、harsh、presence、air、压缩参数和并行 wet 上限，再对角色共振峰、EQ、压缩、激励、声场、响度包络与并行母带执行段内动态控制。
- **basic 层**使用自然停顿扩展、DeepFilterNet 原生 48 kHz 限量降噪和动态并行轻母带，wet 上限通常为 57%–72%。
- **advanced 层**额外加入参考宽带校正、最多 8% 的真实辅音/呼吸细节保护和动态自然度母带，wet 上限通常为 69%–84%。参考细节仅在 AI 人声存在对应活动时加入，避免向空白段注入参考噪声。
- 旧 vocalfloor 硬式深衰减改为带前视、长保持和软膝的自然停顿扩展，最多衰减 6 dB；0.5 秒以内句内停顿会自动桥接，避免切掉起音、呼吸、混响尾或形成明显的 AI 式断句。
- 最终混音默认将 AI 人声提升 1.8 dB、伴奏降低 0.7 dB，并保留约 1.5 dBFS 峰值余量，改善人声偏小的问题。

### 🧩 创建流程、编辑器与 API

- 创建任务页新增自然修音、AI 对齐、AI 角色共振峰、AI EQ、AI Compressor、AI Exciter、Stereo 与 AI 响度包络八项独立滑块，并记忆上次设置。
- Audio Editor Lite 的局部重推理同步支持上述八项参数；高级层以当前片段裁出的原始干声作为参考，增强失败时仍保留重推理结果。
- 桌面桥接、FastAPI 请求模型、前端 API 类型和模拟接口统一增加八项 0–1 参数，服务端会处理非法值、越界值与默认值。
- 处理日志新增句段数、音素锚点数、F0 引导点数、`DurationTier` 点数、对齐偏差、最大伸缩比例、F0 修音幅度和参考相关性，便于定位处理过重的片段。

### 🗑️ 工程与主题文件清理

- 删除音频编辑工程时，会先安全删除对应工程目录及以工程 ID 为前缀的专属渲染缓存，再删除工程记录；其他工程缓存和工程外原始素材不会被误删。
- 工程 ID 会执行目录穿越、符号链接和根目录边界检查；文件清理失败时保留工程记录，用户释放占用后可重新删除。
- 更换、保存或重置自定义壁纸时，会删除已被替换的旧图片或 MP4；未保存的临时壁纸被再次替换时也会清理。
- 壁纸删除接口只允许操作应用主题媒体目录中的普通文件，拒绝外部绝对路径、目录穿越、网络地址和 `data:` URI。

### 📦 安装器与发布资源

- `.venv-vocal` 新增 `praat-parselmouth==0.4.6`，用于跨模型通用的 Praat PSOLA 对齐与自然修音。
- PyInstaller 和发布构建清单新增 `vocal_tuning_worker.py` 与 `inference_naturalizer.py`；安装器会校验推理自然化、自然修音、增强 worker 和 `runtime.ready` 均已正确释放。
- 安装器会把 `README.md`、`release_notes_v026.md` 与 `docs/api.md` 一起释放到安装目录，方便离线查看功能、升级说明和 API 契约。

### ✅ 版本同步

- 应用本体、Python 项目、前端包、两份锁文件、Windows EXE 版本资源、Inno Setup、安装器文档和 API 文档均同步为 **v0.0.26**。
- 作品、模型、编辑工程、主题和其他用户数据继续沿用原数据目录，覆盖升级不会清除已有内容。
