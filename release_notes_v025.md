## v0.0.25 · 作品播放体验与分卷安装资源完善

> 本次更新把这一轮安装器、播放器、歌词搜索和版本同步改动统一归入 **v0.0.25**。重点是让安装更可控、离线资源更完整，同时让作品播放页的歌词 API 搜索真正停在“用户自己选择第几个结果”这一步。

> [!IMPORTANT]
> v0.0.25 继续使用分卷安装包。请同时下载 `XB-SVCB-Setup.exe` 和同版本的全部 `XB-SVCB-Setup-*.bin`，放在同一目录后再运行安装程序；缺少任意分卷都无法完整释放内置资源。

### 🎵 作品播放页与歌词选择

- 作品库进入独立作品播放页后，播放体验补齐进度、音量、歌词跟随高亮、点击歌词跳转和导出控制。
- 播放页支持导入本地 LRC 歌词，也支持通过曲库 API 搜索歌词候选歌曲。
- 歌词 API 来源可由用户在 QQ 音乐、网易云音乐、酷我音乐之间选择，播放页的 API 下拉框已按现有界面风格美化。
- 搜索结果会保留多条候选，用户点击某一条只会选中该版本；只有再次点击“获取选中歌曲歌词”才会按对应 `n` 序号获取歌词，不再默认第一条或点选后立即请求。
- 选中结果会显示当前歌曲序号、歌名、歌手和专辑信息，避免多个同名版本时误取歌词。
- 每首作品可导入图片或 MP4 MV 画面，关联信息按作品保存，重新打开播放页会自动恢复。
- MV 视频改为跟随音频的统一时间轴，播放、暂停、拖动和播放中的时间漂移都会自动同步；视频模式不再显示中央唱片、标题遮罩，完整画面只保留右上角紧凑的更换与移除控制。
- 播放页的 MV 舞台与歌词面板改为稳定的响应式高度和不透明主题底色；长歌词、搜索结果及播放跟随只在面板内部滚动，不再把播放页面持续向下撑长或带动整页跳动。

### 🔎 歌词后端与曲库来源

- 播放页调用的桌面桥接后端继续使用统一的音乐服务，搜索阶段返回多条候选，歌词获取阶段按 `msg + n + source + songId/rid` 定位具体版本。
- QQ、网易云、酷我三种来源保持独立选择；搜索关键词和候选序号会随用户选择保留，减少同名歌曲取错歌词的概率。
- 修复作品播放页选择酷我搜索结果后因 RID 详情接口异常而无法取得歌词的问题；酷我歌词与多人合唱统一使用同一次搜索的关键词和候选序号获取，并对上游偶发的空白/非 JSON 响应执行受限重试。
- 搜索不到歌词或 API 返回空歌词时，会保留清晰提示，用户仍可切换曲库、换关键词或导入本地 LRC。

### 📦 安装器与前置依赖

- 前置依赖从“安装器自动安装”改为“用户辅助模式”：安装器负责检测 Python、Git、Visual C++ Build Tools、CUDA Toolkit、显卡驱动等系统组件，并提供对应下载按钮。
- CUDA 下载入口只在检测到 NVIDIA 显卡时显示：RTX 50 系直接提供 CUDA 12.8 官方下载入口，RTX 40 系及以下直接提供 CUDA 12.1 官方下载入口；AMD、Intel 和纯 CPU 设备不显示 CUDA 项。
- uv 改为在用户安装好 Python 后自动安装：预检查脚本和运行环境搭建脚本都会在未检测到 uv 时调用当前 Python 的 pip 安装/修复 uv，并把 Scripts 目录写入用户 PATH。
- 用户完成前置安装后可回到安装器重新检测；已安装项目会直接跳过，不再重复处理。
- 修复前置检测页使用 Inno Setup 不支持的用户目录和 Program Files 常量导致的运行时错误，用户目录改为读取 `USERPROFILE`，Program Files 路径统一使用 `{autopf}`。
- FFmpeg 改为安装分卷自带。系统 PATH 已有可用 `ffmpeg` 时跳过释放；未检测到时释放到安装目录 `tools\ffmpeg`，并自动配置 `PATH`、`XB_FFMPEG_DIR` 与 `FFMPEG_HOME`。
- So-VITS-SVC、DDSP-SVC、SeedVC 引擎源码随安装分卷自带；安装时检测到本地已释放源码会跳过 Git/ZIP 获取，只在目标机器上创建对应 Python 隔离环境。
- 安装日志继续写入 `{app}\install_logs`，便于排查用户机器上的前置依赖和环境搭建问题。

### 🧩 分卷发布约束

- 安装器继续输出 `XB-SVCB-Setup.exe` 与 `XB-SVCB-Setup-*.bin` 分卷文件。
- `DiskSliceSize` 固定为 `1,900,000,000` 字节，构建后会校验每个分卷小于 2 GiB，方便上传到 GitHub Release。
- 发布 Release 时必须上传同版本 EXE 和全部 BIN；用户本地也必须把它们放在同一目录再安装。

### ✅ 版本同步

- 应用本体、Python 项目、前端包、锁文件、Windows EXE 版本资源、Inno Setup、安装器文档和 API 文档均同步为 **v0.0.25**。
- 项目自有代码的开源协议由 MIT 调整为 **GNU GPL v3.0 only（GPL-3.0-only）**；安装器新增 GPLv3 许可确认页，并在安装目录保留完整 `LICENSE`。第三方组件和模型继续遵循各自原始协议。
- 安装器会把 `README.md`、`release_notes_v025.md` 与 `docs/api.md` 一起释放到安装目录，便于离线查看功能、升级说明和 API 契约。
- 已安装旧版可覆盖升级；模型、作品、下载素材、编辑工程、API 上传、设置、消息状态和主题媒体继续使用原数据目录。
