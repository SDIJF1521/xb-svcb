[project]
name = "app"
version = "0.0.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx[http2]>=0.28.1",
    "pywebview>=6.2.1",
]
